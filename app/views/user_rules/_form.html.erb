<%= simple_form_for(@user_rule, url: [@workflow, @user_rule]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :condition, label: 'Condition' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
