<div class="row">
<div class="col-md-12">
  <%= form_for :search, url: search_project_subjects_path(@project) do |form| %>
    <div class="form-row">
      <h3>Find Subject</h3>
      <br/>
      <div class="form-group col-md-8" style="padding-left: 0;">
        <%= form.text_field :id, class: 'form-control', placeholder: 'Subject id' %>
      </div>
      <div class="form-group col-md-4">
        <%= form.submit 'Search', class: 'btn btn-primary pull-left' %>
      </div>
    </div>
    <br />
    <dl class="dl-horizontal">
      <dt>Last Five Reduced:</td>
      <dd><%= @project.last_n_subjects(5, @project.subject_reductions).map{ |id| link_to(id, project_subject_path(@project, id)) }.join(', ').html_safe %></dd>
      <dt>Random Five:</td>
      <dd><%= @project.random_n_subjects(5).map{ |id| link_to(id, project_subject_path(@project, id)) }.join(', ').html_safe %></dd>
    </dl>
  <% end  %>
</div>
</div>